{
  "name": "chat-surface-bench",
  "install": "pnpm install --frozen-lockfile && pnpm seed",
  "terminals": [
    {
      "name": "chat (3000)",
      "command": "pnpm dev",
      "description": "Next.js dev server for the chat bench on http://localhost:3000"
    }
  ],
  "ports": [
    { "name": "chat", "port": 3000 }
  ]
}
